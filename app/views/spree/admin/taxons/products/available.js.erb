<h4><%= t(:available_products) %></h4>
<table class="index">
  <thead>
    <tr>
      <th><%= t(:name) %></th>
      <th><%= t(:action) %></th>
    </tr>
  </thead>
  <tbody>
    <% @products.each do |product| %>
      <tr id="<%= dom_id(product, :sel) %>">
        <td><%= product.name %></td>
        <td class="actions">
          <%= link_to icon(:add) + ' ' + t(:select),
                            select_admin_taxonomy_taxon_product_path(@taxon.taxonomy, @taxon, product),
                           :remote => true, :class => 'iconlink' %>
        </td>
      </tr>
    <% end %>
    <% if @products.empty? %>
     <tr><td colspan="3"><%= t(:no_match_found) %>.</td></tr>
    <% end %>
  </tbody>
</table>
